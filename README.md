# Validation Runner

A Python project for software validation and test automation.

## Project Structure

- `src/` - core project code
- `tests/` - pytest test files
- `scripts/` - helper automation scripts

