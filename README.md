# Validation Runner

A learning-by-doing Python project for software validation / QA automation interview preparation.

## Current Status

Layer 0: Environment & Workflow Setup

## Project Structure

- `src/` - core project code
- `tests/` - pytest test files
- `scripts/` - helper automation scripts

## Setup

```bash
source .venv/bin/activate
pip install -r requirements.txt
```